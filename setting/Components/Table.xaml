<UserControl
    x:Class="Citadel.Setting.Components.SettingTable"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Citadel.Setting.Components"
    Background="{DynamicResource TransparentBrush}"
    Foreground="{DynamicResource Fg}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ScrollBar.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DataGrid
        x:Name="InnerTable"
        AutoGenerateColumns="False"
        Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
        BorderThickness="0"
        CanUserAddRows="False"
        CanUserDeleteRows="False"
        CanUserReorderColumns="False"
        CanUserResizeColumns="{Binding CanUserResizeColumns, RelativeSource={RelativeSource AncestorType=UserControl}}"
        CanUserResizeRows="False"
        CanUserSortColumns="{Binding CanUserSortColumns, RelativeSource={RelativeSource AncestorType=UserControl}}"
        ColumnWidth="{Binding ColumnWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
        EnableColumnVirtualization="True"
        EnableRowVirtualization="True"
        FontSize="{DynamicResource SettingBodyFontSize}"
        Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
        GridLinesVisibility="All"
        HeadersVisibility="Column"
        HorizontalGridLinesBrush="{DynamicResource Border}"
        HorizontalScrollBarVisibility="Auto"
        IsReadOnly="True"
        ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
        RowHeaderWidth="0"
        SelectionMode="Single"
        SelectionUnit="FullRow"
        SnapsToDevicePixels="True"
        VerticalGridLinesBrush="{DynamicResource Border}"
        VerticalScrollBarVisibility="Auto">
        <DataGrid.Resources>
            <!-- Keep DataGrid's specialized ScrollViewer template, but render
                 every scrollbar through the shared Citadel primitive. -->
            <Style TargetType="{x:Type ScrollBar}"
                   BasedOn="{StaticResource SettingScrollBarStyle}" />
            <Style TargetType="{x:Type ScrollViewer}">
                <Setter Property="local:ScrollBarAutoFade.IsEnabled" Value="True" />
            </Style>
            <Style TargetType="{x:Type DataGridColumnHeader}">
                <Setter Property="Background" Value="{DynamicResource Hover}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
                <Setter Property="BorderThickness" Value="0,0,1,2" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{DynamicResource Fg}" />
                <Setter Property="HorizontalContentAlignment"
                        Value="{Binding HeaderHorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type local:SettingTable}}}" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Padding" Value="10,6" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                                <Grid VerticalAlignment="Center">
                                    <ContentPresenter
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                    <TextBlock
                                        x:Name="SortIndicator"
                                        Margin="0,0,4,0"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Foreground="{TemplateBinding Foreground}"
                                        Text="▲"
                                        Visibility="Collapsed" />
                                    <Thumb
                                        x:Name="PART_RightHeaderGripper"
                                        Width="7"
                                        HorizontalAlignment="Right"
                                        Background="Transparent"
                                        Cursor="SizeWE"
                                        Focusable="False">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border Background="Transparent" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="SortDirection" Value="Ascending">
                                    <Setter TargetName="SortIndicator" Property="Text" Value="▲" />
                                    <Setter TargetName="SortIndicator" Property="Visibility" Value="Visible" />
                                </Trigger>
                                <Trigger Property="SortDirection" Value="Descending">
                                    <Setter TargetName="SortIndicator" Property="Text" Value="▼" />
                                    <Setter TargetName="SortIndicator" Property="Visibility" Value="Visible" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="{x:Type local:SettingButton}">
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <Style TargetType="{x:Type DataGridCell}">
                <Setter Property="Background" Value="{DynamicResource TransparentBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
                <Setter Property="BorderThickness" Value="0,0,1,1" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Foreground" Value="{DynamicResource Fg}" />
                <Setter Property="HorizontalContentAlignment"
                        Value="{Binding CellHorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type local:SettingTable}}}" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Padding" Value="10,5" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type DataGridCell}">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                                <ContentPresenter
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="{x:Type DataGridRow}">
                <Setter Property="Background" Value="{DynamicResource TransparentBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource Fg}" />
                <Setter Property="MinHeight" Value="40" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource Hover}" />
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{DynamicResource Selected}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </DataGrid.Resources>
    </DataGrid>
</UserControl>
