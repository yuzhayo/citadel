<UserControl
    x:Class="Citadel.Setting.Components.SettingPasswordField"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{DynamicResource Bg}"
    Foreground="{DynamicResource Fg}">
    <Border
        x:Name="Surface"
        Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
        BorderThickness="1"
        CornerRadius="{DynamicResource SidebarItemCornerRadius}"
        Padding="{DynamicResource SettingControlPadding}">
        <Border.Style>
            <Style TargetType="{x:Type Border}">
                <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
                <Style.Triggers>
                    <DataTrigger
                        Binding="{Binding IsKeyboardFocusWithin, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource Accent}" />
                    </DataTrigger>
                    <DataTrigger
                        Binding="{Binding IsEnabled, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Value="False">
                        <Setter Property="Opacity" Value="{DynamicResource DisabledOpacity}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <PasswordBox
            x:Name="InnerPassword"
            Background="{DynamicResource TransparentBrush}"
            BorderThickness="0"
            CaretBrush="{DynamicResource Fg}"
            FontSize="{DynamicResource SettingBodyFontSize}"
            Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
            PasswordChanged="InnerPassword_PasswordChanged" />
    </Border>
</UserControl>
