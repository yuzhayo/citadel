<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Citadel.Setting.Components">
    <Style x:Key="SettingTableActionsStyle" TargetType="{x:Type local:SettingTableActions}">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SettingTableActions}">
                    <Grid>
                        <ContentPresenter
                            x:Name="PrimaryPresenter"
                            Content="{TemplateBinding PrimaryContent}"
                            ContentTemplate="{TemplateBinding PrimaryContentTemplate}"
                            HorizontalAlignment="Left"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        <ContentPresenter
                            x:Name="SecondaryPresenter"
                            Content="{TemplateBinding SecondaryContent}"
                            ContentTemplate="{TemplateBinding SecondaryContentTemplate}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="SecondaryContent" Value="{x:Null}">
                            <Setter TargetName="PrimaryPresenter" Property="HorizontalAlignment" Value="Center" />
                        </Trigger>
                        <Trigger Property="PrimaryContent" Value="{x:Null}">
                            <Setter TargetName="SecondaryPresenter" Property="HorizontalAlignment" Value="Center" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
