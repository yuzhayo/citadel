<Button
    x:Class="Citadel.Setting.Components.SettingButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Citadel.Setting.Components"
    Background="{DynamicResource Hover}"
    BorderThickness="0"
    FontSize="{DynamicResource SettingBodyFontSize}"
    Foreground="{DynamicResource Fg}"
    HorizontalContentAlignment="Center"
    Padding="{DynamicResource SettingControlPadding}"
    VerticalContentAlignment="Center">
    <Button.Template>
        <ControlTemplate TargetType="{x:Type local:SettingButton}">
            <Border
                x:Name="Surface"
                Background="{TemplateBinding Background}"
                BorderBrush="{DynamicResource TransparentBrush}"
                BorderThickness="1"
                CornerRadius="{DynamicResource SidebarItemCornerRadius}"
                Padding="{TemplateBinding Padding}">
                <ContentPresenter
                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                    VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Surface" Property="Background" Value="{DynamicResource Selected}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="Surface" Property="Background" Value="{DynamicResource Card}" />
                </Trigger>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter TargetName="Surface" Property="BorderBrush" Value="{DynamicResource Accent}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="{DynamicResource DisabledOpacity}" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Button.Template>
</Button>
